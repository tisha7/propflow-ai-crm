import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export default function LeadsPage() {
  return (
    <ModulePlaceholder
      title="Leads"
      description="Capture, qualify, and route leads to the right agent."
      checkpoint="Checkpoint 3"
    />
  );
}
